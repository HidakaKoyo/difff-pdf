- 基本方針: 既存テキストモードの挙動は不変を厳守（`split_text`/比較/HTML出力）。
- 変更は小さく、影響範囲を限定。無関係な既存問題は勝手に修正しない。
- 実装ログは `docs/implementation-log.md` に時系列追記。
- 依存管理は混在禁止。ロック/再現性を重視。
- コミットメッセージは絵文字 + Conventional Commits 形式（AGENTS.md準拠）。