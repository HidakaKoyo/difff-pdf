1. 変更ファイルを確認し、要求範囲外の差分がないことを確認する。
2. `perl -c` と `uv run ... py_compile` を実行して構文エラーがないことを確認する。
3. 必要に応じてローカルで `http://localhost:8000/difff.pl` からテキスト比較→PDF比較を確認する。
4. `docs/implementation-log.md` に意思決定と結果を追記する。
5. 実行コマンドと結果（成功/失敗）を報告する。